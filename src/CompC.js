import { useState } from "react";
const CompC = (props) =>{
  return (
  <div>
      <div>{props.propoC.key1} in C</div>
      <div>{props.propoC.key2} in C</div>
      <div>{props.propoC.key3} in C</div>

  </div>
  );
};

export default CompC;